<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">	
  <!-- 쿼리가 담기는 설정 파일이다 -->
  
  <mapper namespace="BrandTheme">
	  <insert id="insertBrandTheme" parameterType="brandTheme"> <!-- DAO에서 BrandVO vo(파라미터)에 담아져있기에 parameterType="Brand"생략가능함 -->
	  <![CDATA[
	  		insert into BrandTheme(bm_no, bt_no, bt_color, bt_font, bt_logo) 
	  		values ((select nvl(max(bt_no),0)+1 from BrandTheme), #{bm_no}, #{bt_color}, #{bt_font}, #{bt_logo})
	  ]]>
	  </insert>	
	  <!-- '<', '>'는 비교 연산자로 인식하지 않고 여기에서는 꺽쇠로 본다. 설정을 <![CDATA[]]>로 --> <!-- // SQL문 - nvl: Null값이면 0으로 실행해라 -->
	  <!-- ${}는 변수를 바로 가져온다. #{}은 실행되어지는 시점에 변수를 가져온다. 둘이 같다고 생각해도 상관없다. -->
  	  
	  <!-- <select id="getBrandList_T" resultType="Brand">	resultType="Brand" 반환받는 타입을 명시하고 있어야 한다. 짧게 별명alias로 Brand로
	  <![CDATA[
	  	select * from Brand where title like '%'||#{searchKeyword}||'%' order by seq desc
	  ]]>
	  </select>
	  
	   
	  <select id="getBrandList_C" resultType="Brand">
	  <![CDATA[
	  	select * from Brand where content like '%'||#{searchKeyword}||'%' order by seq desc
	  ]]>
	  </select> -->
	  
	  <!-- Mybatis가 제공하는 동적(Dynamic) SQL 쿼리문을 추가적으로 제공해줌. Spring(X) -->
	  <!-- resultType="Brand" 반환받는 타입을 명시하고 있어야 한다. 짧게 별명alias로 Brand로 -->
	  <select id="getBrandThemeList" resultType="brandTheme">	
		select * 
	  	from BrandTheme 
	  	where 1 = 1
	  	<if test="searchCondition == 'BM_NO'">
	  		and BM_NO like '%'||#{searchKeyword}||'%' 
	  	</if>
	  	<if test="searchCondition == 'BT_NO'">
	  		and BT_NO like '%'||#{searchKeyword}||'%' 
	  	</if>
	  	order by bt_no desc
	  </select>
	  
	  
	  <!-- List 보여주기 -->
	  <select id="getBrandTheme" resultType="brandTheme">
	  <![CDATA[
	  	select * from BrandTheme where bt_no = #{bt_no}
	  ]]>
	  </select>
	  
	  
	  <update id="updateBrandTheme">
	  <![CDATA[
	  	update BrandTheme set bm_no = #{bm_no}, bt_no = #{bt_no}, bt_color = #{bt_color}, bt_font = #{bt_font}, 
	  	bt_logo = ${bt_logo} where bt_no = #{bt_no}
	  ]]>
	  </update>
	  
	  
	  <delete id="deleteBrandTheme">
	  <![CDATA[
	  	delete from BrandTheme where bt_no = #{bt_no}
	  ]]>
	  </delete>
	  
  </mapper>
	  
    	
			
	