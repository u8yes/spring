<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="Keep">

	<insert id="insertKeep" parameterType="keep">
	<![CDATA[
		insert into keep(k_no, um_no, p_pno, bm_no) values((select nvl(max(k_no),0)+1 from keep),
					 #{um_no}, #{p_pno}, #{bm_no}) 
	]]>
	</insert>

	 <select id="getKeepList" resultType="keep">
		<![CDATA[
		select * from keep order by k_no asc
		]]>
	</select> 
	
	 <select id="getKeep" resultType="keep">
	<![CDATA[
		select * from keep where k_no = #{k_no}
	]]>
	</select> 
	
	 <update id="updateKeep">
	<![CDATA[
		update keep set um_no = #{um_no}, p_pno = #{p_pno} where bm_no = #{bm_no}
	]]>
	</update> 
	
	
	<delete id="deleteKeep">
	<![CDATA[
		delete from keep where k_no = #{k_no}
	]]>
	</delete>
	
</mapper>



	
	
	

	

 