<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  <mapper namespace="Board">
  	<insert id="insertBoard" parameterType="board"> <!-- parameter(매개변수) -->
  		<![CDATA[      
  		insert into board(seq,title,writer,content)
  		values((select nvl(max(seq),0)+1 from board), #{title}, #{writer}, #{content})
  		]]> <!-- 안쪽 코드는 엘리먼트를 쿼리문내 비교연산자로 인식   -->
  	</insert>
<!--   	<select id="getBoardList_T" resultType="board"> select문은 결과를 받는 문구  무조건 resultType 필요
  		<![CDATA[  
  		select * from board where title like '%'||#{searchKeyword}||'%' order by seq desc
  		]]>
  	</select>
  	<select id="getBoardList_C" resultType="board"> select문은 결과를 받는 문구  무조건 resultType 필요
  		<![CDATA[  
  		select * from board where content like '%'||#{searchKeyword}||'%' order by seq desc
  		]]>
  	</select> -->
  	 <select id="getBoard" resultType="board"> <!-- select문은 결과를 받는 문구  무조건 resultType 필요-->
  		<![CDATA[  
  		select * from board where seq = #{seq}
  		]]>
  	</select>
  	<select id="getBoardList" resultType="board"> <!-- 동적SQL, MyBatis자체 제공 -->
 
  		select * 
  		from board 
  		where 1 = 1 
  		<if test="searchCondition == 'TITLE'"> 
  		and title like '%'||#{searchKeyword}||'%' 
  		</if>
  		<if test="searchCondition == 'CONTENT'">
  		and content like '%'||#{searchKeyword}||'%'
  		</if>
  		order by seq desc

  	</select>
  	<update id="updateBoard">
  	  	<![CDATA[ 
  	  	 update board set title = #{title}, content = #{content} where seq = #{seq}
  		]]>
  	</update>
  	<delete id="deleteBoard">
  	  	<![CDATA[ 
  	  	delete from board where seq = #{seq}
  		]]>
  	</delete>
  </mapper>


